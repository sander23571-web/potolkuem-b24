# Проект «Потолкуем?» — контекст для Claude

> **Ключевая фраза:** «Осмотрись в проекте»
> Прочитай этот файл → потом `b24-api-patterns.md` → и ты знаешь всё.

---

## Суть проекта

**Исполнитель:** БюроОБП (bobp.ru)
**Клиент:** a-dat.ru («Потолкуем?») — производитель настольных игр для развития речи (20+ игр)

**Портал:** `potolkuem.bitrix24.ru` · тариф «Профессиональный»
**Вебхук:** `https://potolkuem.bitrix24.ru/rest/134/gj6y27ehe0f42jeb/`
**Период работ:** апрель — июнь 2026

---

## Смарт-процессы (все)

| Название | typeId | entityTypeId | categoryId | entityId (UF) |
|---|---|---|---|---|
| Отдел разработки | 10 | 1044 | 16 | CRM_10 |
| Выставки | 12 | 1048 | 20 | CRM_12 |
| Ведущие | 14 | 1052 | 22 | CRM_14 |
| Выход ведущего | 16 | 1056 | 24 | CRM_16 |
| Игровая сессия | 18 | 1060 | 26 | CRM_18 |
| Логистика | 20 | 1064 | 28 | CRM_20 |
| Согласия | 22 | 1068 | 30 | CRM_22 |
| **Расходы** | **24** | **1070** | **32** | **CRM_24** |
| **Точки продаж** | **26** | **1072** | **44** | **CRM_26** |

> ⚠️ `entityId` для `userfieldconfig.add` = `CRM_{typeId}`, **НЕ** `CRM_{entityTypeId}`.
> Полные API-паттерны: **`b24-api-patterns.md`**

### Стадии СП Точки продаж (DT1072_44:*)

| STATUS_ID | Название | id |
|---|---|---|
| DT1072_44:NEW | Переговоры | 724 |
| DT1072_44:PREPARATION | Договор на согласовании | 726 |
| DT1072_44:CLIENT | Активная реализация | 728 |
| DT1072_44:UC_PAUSED | Приостановлена | 734 |
| DT1072_44:SUCCESS | Завершена | 730 |
| DT1072_44:FAIL | Отказ | 732 |

### UF-поля СП Точки продаж (CRM_26)

| Поле | Тип | id | Метка |
|---|---|---|---|
| ufCrm26Address | string | 596 | Адрес точки |
| ufCrm26Commission | double | 598 | Комиссия % |
| ufCrm26DateStart | date | 600 | Дата начала договора |
| ufCrm26DateReport | date | 602 | Дата следующего отчёта |
| ufCrm26ContractTerms | string | 604 | Условия договора |
| ufCrm26Warehouse | string | 606 | Склад |

### Записи Точек продаж

| id | Название | Компания id | Стадия |
|---|---|---|---|
| 2 | Магазин "Листва" | 18 | Активная реализация |
| 4 | Магазин "Gold Fish" | 20 | Активная реализация |
| 6 | Магазин ЦДМ | 22 | Активная реализация |

---

## Воронки сделок

| ID | Название | Назначение |
|---|---|---|
| 0 (default) | Ателье (Продажа) | Стадии не настроены. Для B2B-продаж с циклом |
| 18 | Розничные продажи | Все розничные продажи: выставки, онлайн, соцсети (SOURCE_ID разделяет каналы) |
| 24 | (пустая) | Записи мигрированы в СП Точки продаж |
| 26 | Мероприятия B2C | Открытые игровые сессии |
| 28 | Ателье (Производство) | Производство кастомных игр |
| 30 | Мероприятия B2B | Корпоративные мероприятия |
| 32 | Реализация точек | Периодические отчёты от точек продаж (дочерние к СП Точки продаж) |

### Стадии кат.32 Реализация точек

| STATUS_ID | Название |
|---|---|
| C32:NEW | Отчёт получен |
| C32:PREPARATION | Счёт выставлен |
| C32:WON | Оплачено |
| C32:LOSE | Спорная ситуация |

---

## АРХ МОСКВА 2026 — импорт (май–июнь 2026)

Первая выставка, которую полностью завели в CRM через API.

### Созданные записи

| Сущность | Кол-во | Ключевые данные |
|---|---|---|
| Выставка (СП 1048) | 1 | id=4, название «АРХ МОСКВА» |
| Выходы ведущего (СП 1056) | 10 | parentId1048=4, даты 27–30 мая |
| Сделки (воронка 18) | 59 | #232–#350 чётные, #256 удалена |
| Расходы (СП 1070) | 6 | parentId1048=4, итого 530 085 руб. |

### Финансовый итог выставки

| | |
|---|---|
| Выручка | 300 689 руб. |
| Расходы | 530 085 руб. |
| Убыток | -229 396 руб. |

### Связи сделок

- **Ведущие → Сделки:** `PARENT_ID_1052` (uppercase, через `crm.deal.update`) — работает
- **Аня → Выставка:** `PARENT_ID_1048=4` — у её 20 сделок (#312–#350)
- `parentId1052` camelCase в `crm.deal.add` — **молча игнорируется**
- `CATEGORY_ID` в `crm.deal.update` — **молча не меняет воронку**

### Ведущие на выставке

| Ведущий | id (СП 1052) | Сделок | Выручка |
|---|---|---|---|
| Макарова Ольга | 28 | 19 | 93 049 |
| Анна (сотрудник) | — | 20 | 88 750 |
| Музеник Даниил | 20 | 12 | 77 530 |
| Сухомицкая Екатерина | 14 | 4 | 19 660 |
| Шестикова Екатерина | 6 | 3 | 14 900 |
| Звёздочкина Варвара | 26 | 1 | 6 800 |

### Скрипты импорта

| Файл | Что делает |
|---|---|
| `scripts/import-arh-moskva.js` | Создал Выставку, Выходы, 60 Сделок |
| `scripts/import-rashody-arh-moskva.js` | Создал 6 записей Расходов |
| `arh-moskva-bi-brief.md` | Справка для BI-отчёта (данные, ID, SQL) |
| `arh-moskva-report.html` | HTML-отчёт (создан коллегой по брифу) |

---

## non/fictioN 2026 — импорт (июнь 2026)

**Выставка:** 9–12 апреля 2026, Гостиный двор, ул. Ильинка, 4 · id=30

### Созданные записи

| Сущность | Кол-во | Ключевые данные |
|---|---|---|
| Выходы ведущего (СП 1056) | 15 | parentId1048=30, даты 9–12 апреля, id 52–80 |
| Сделки (воронка 18) | 46 | #406–#496 чётные, итого 170 920 руб. |
| Расходы (СП 1070) | 8 | parentId1048=30, id 28–42, итого 324 212 руб. |
| Контакты | 57 | уже были в базе (17.04.2026), привязаны через contactIds |

### Финансовый итог

| | |
|---|---|
| Выручка | 170 920 руб. |
| Расходы | 324 212 руб. |
| Убыток | -153 292 руб. |

### Ведущие

| Ведущий | id | Выходов | Выплата |
|---|---|---|---|
| Шестикова Екатерина | 6 | 2 | 13 588 |
| Макарова Ольга | 28 | 4 | 21 368 |
| Троянова Дарья | 30 | 4 | 18 898 |
| Щурова Наталья | 24 | 2 | 11 946 |
| Алина | 64 | 2 | 8 224 |
| Варя | 66 | 1 | 6 936 |

### Источники данных

| Файл | Содержание |
|---|---|
| `NONFICTION/KPI nonfiction.xlsx` | Ведущие, выходы, продажи |
| `NONFICTION/ЗАТРАТЫ (2).xlsx` | Расходы |
| `NONFICTION/КОНТАКТЫ С ВЫСТАВКИ.xlsx` | 58 B2B-контактов |
| `/tmp/import-nonfiction-deals.js` | Скрипт сделок |
| `/tmp/import-nonfiction-vyhody.js` | Скрипт выходов |
| `/tmp/import-nonfiction-rashody.js` | Скрипт расходов |

---

## ОБРАЗОВАНИЕ И КАРЬЕРА 2026 — импорт (июнь 2026)

**Выставка:** 28.02–01.03.2026, Гостиный двор · id=24

### Созданные записи

| Сущность | Кол-во | Ключевые данные |
|---|---|---|
| Выходы ведущего (СП 1056) | 6 | parentId1048=24, id 82–92, дата 28.02 |
| Сделки (воронка 18) | 15 | #498–#526 чётные, итого 84 095 руб. |
| Расходы (СП 1070) | 11 | parentId1048=24, id 44–64, итого 603 362 руб. |

### Финансовый итог

| | |
|---|---|
| Выручка | 84 095 руб. |
| Расходы | 603 362 руб. |
| Убыток | -519 267 руб. |

### Ведущие

| Ведущий | id | Выплата |
|---|---|---|
| Захаров Егор | 68 | 5 460 |
| Шестикова Екатерина | 6 | 4 880 |
| Макарова Ольга | 28 | 1 880 |
| Луференко Данила | 12 | 3 780 |
| Троянова Дарья | 30 | 5 920 |
| Илья | 70 | 0 |

> ⚠️ Выходы созданы как сводные (1 запись на ведущего), без разбивки по дням — данных не было.
> ⚠️ 10 из 15 сделок без названия игры — только суммы (данных в файле не было).
> ⚠️ Захаров Егор (id=68) и Илья (id=70) — новые ведущие, больше не работают.

### Источники данных

| Файл | Содержание |
|---|---|
| `Образование/KPI (1) (1).xlsx` | Ведущие, продажи, выплаты |
| `Образование/ПРИХОД-РАСХОД (1).xlsx` | Доходы и расходы |
| `Образование/ПРОГРАММА.xlsx` | Расписание |
| `/tmp/import-obrazovanie-rashody.js` | Скрипт расходов |
| `/tmp/import-obrazovanie-deals.js` | Скрипт сделок |

---

## Интеграция сайта → Б24 (Яндекс Cloud Functions)

- **b24-lead** (`d4e9bji829gbk3i0mt4t`) — формы сайта → Лиды/Ведущие
- **b24-order** (`d4e9plotesn2rm32jo5f`) — заказы → Контакт + Сделка
- Задеплоены, готовы.
- ⚠️ Разработчик сайта potolkuem.pro **не реагирует** — к функциям не подключён

## CardScanner PWA

- Сканирование визиток → OCR (Yandex Vision + YandexGPT) → Контакт в Б24
- Флоу согласия 152-ФЗ через QR-токен
- **URL:** https://potolkuem.bobp.ru · Сервер: Beget VPS `155.212.143.68`
- Задеплоен, ожидает тестирования
- ⚠️ Верификация карты Яндекс Cloud не пройдена (грант ₽3 000 ожидает)

## consent-service

- Node.js/Express, `GET /consent?cid=X&src=source`
- Записывает согласие в СП Согласия (1068), rate limit 10/min

## report-app — Дашборд выставок (июнь 2026)

✅ **Задеплоен и работает:** `https://db-talk.bobp.ru/report`

### Инфраструктура

- **Сервер:** `155.212.143.68` (продакшн, там же CardScanner, b24proxy)
- **SSH:** `ssh -p 2222 root@155.212.143.68`
- **Директория на сервере:** `/root/projects/talk-report/`
- **Порт:** 3002 · **PM2:** `pm2 restart report-app`
- **Nginx:** `/etc/nginx/sites-available/db-talk-bobp` (включён, SSL от certbot)
- **SSL:** Let's Encrypt, истекает 2026-09-26, автообновление настроено

### Доступ

- **URL:** `https://db-talk.bobp.ru/report`
- **Логин:** `admin`
- **Пароль:** `JGBDG7lVRqTjeTkg` (в `/root/projects/talk-report/.env`)

### Исходники

Код хранится в репозитории `/root/projects/talk` (текущая машина `46.173.20.187`).
При изменениях — копировать на продакшн:
```bash
scp -P 2222 report-app/index.js report-app/b24.js report-app/render.js \
    root@155.212.143.68:/root/projects/talk-report/
ssh -p 2222 root@155.212.143.68 'pm2 restart report-app'
```

| Файл | Назначение |
|---|---|
| `report-app/index.js` | Express + Basic Auth + маршруты |
| `report-app/b24.js` | Клиент Б24 API + in-memory кеш 5 мин |
| `report-app/render.js` | Генератор HTML: KPI, Chart.js, расходы со ссылками на Б24, сделки, P&L |
| `report-app/.env` | PORT=3002, REPORT_USER, REPORT_PASSWORD, B24_WEBHOOK |

### Функционал (актуально на 30.06.2026)

- Список выставок в dropdown + кнопка «Все выставки» → `/report/compare`
- KPI: выручка, расходы, P&L, сделок, ведущих
- Графики Chart.js — все кликабельны (открывают карточку в Б24):
  - Выручка по ведущим (bar) → клик → `/crm/type/1052/details/{id}/`
  - Структура расходов (doughnut) → клик → `/crm/type/1070/details/{id}/`
  - Выручка по дням (bar, только просмотр)
- Заголовок выставки — ссылка на карточку выставки (`/crm/type/1048/details/{id}/`)
- Таблица расходов: ссылка «Открыть» → карточка Расхода
- Таблица сделок: ссылка «Открыть» → `/crm/deal/details/{ID}/`
- Таймлайн выходов: имя ведущего → его карточка, название выхода → карточка Выхода
- Кнопка «Обновить данные» — сбрасывает кеш

### Страница сравнения `/report/compare`

- Итоговые KPI по всем выставкам (сумма)
- Grouped bar chart (Выручка vs Расходы) → клик на столбец → дашборд выставки
- Сводная таблица: название (ссылка), даты, выручка, расходы, P&L, сделок
- Данные: лёгкий fetch (только expenses + deals через PARENT_ID_1048) без полного кеша

### Фильтрация сделок — важно

Сделки тянутся двумя запросами:
1. `PARENT_ID_1048: id, CATEGORY_ID: 18` — сделки напрямую к выставке
2. `PARENT_ID_1052: hostIds, CATEGORY_ID: 18, >=CLOSEDATE: begindate, <=CLOSEDATE: closedate`
   — сделки ведущих, фильтрованные по датам выставки (иначе тянутся все их сделки за всё время)

Причина двух запросов: `parentId1048` (lowercase) в `crm.deal.add` молча игнорируется —
связь с выставкой работает только через `crm.deal.update` с `PARENT_ID_1048` (uppercase).
У старых импортов (АРХ МОСКВА) update не делался → первый запрос для них пустой.

### Важные грабли

- `realm` в express-basic-auth должен быть ASCII — кириллица вызывает ERR_INVALID_CHAR
- Этот репозиторий (`46.173.20.187`) и продакшн-сервер (`155.212.143.68`) — **разные машины**
- certbot нужно запускать именно на `155.212.143.68`
- Порт 443 на `155.212.143.68` — xray (VLESS REALITY), nginx на 80 делает 301→HTTPS через certbot

---

## GOTOVO 2026 — импорт (июнь 2026)

**Выставка:** 24–27.03.2026 · id = уточнить после импорта

| Файл | Содержание |
|---|---|
| `GOTOVO/KPI (1) (3).xlsx` | Ведущие, продажи |
| `GOTOVO/Выплаты ведущим.xlsx` | Выплаты |
| `GOTOVO/ПРИХОД-РАСХОД.xlsx` | Доходы и расходы |
| `scripts/import-gotovo-2026.js` | Скрипт сделок |

> Скрипт готов, статус импорта — уточнить.

---

## Автонаблюдатель в задачах

**Файл:** `../Bitrix24/ENVPro/ЗАДАЧИ_НАБЛЮДАТЕЛЬ.md`
**Статус:** ✅ cron активен (21.06.2026)

Автодобавление Агеевой-Дзукаевой как наблюдателя при создании задач на 6 сотрудников.
Polling через cron раз в минуту (`tasks.task.list` за последние 3 минуты).

---

## requireResult для задач Юмангуловой Алины (июль 2026)

**Статус:** ✅ выполнено и автоматизировано (01.07.2026)

**Что сделано:**
- AI «Марта» вместо флага «обязательный результат» добавила текст результата «Стандартная форма подтверждения выполнения работы» в 4 задачи (IDs 512, 514, 516, 520) — удалены через REST 3.0 `tasks.task.result.delete`
- Поставлен `requireResult=True` на все 50 задач где Алина (id=132) — участник в любой роли
- Cron-скрипт задеплоен: новые задачи с Алиной автоматически получают флаг

**Технические детали:**
- REST 3.0 URL: `/rest/api/` вместо `/rest/` (нужен для `requireResult`, `tasks.task.result.*`)
- `tasks.task.result.delete` — параметр `{"id": resultId}`, не `taskId`
- `requireResult` — поле в `tasks.task.update` (fields) и `tasks.task.get` (select), только REST 3.0
- `tasks.task.update` REST 3.0 возвращает `{"result": {"result": true}}` (не `item`)
- Алина Юмангулова: user id=132

**Скрипты:**
| Файл | Назначение |
|---|---|
| `scripts/task_require_result_cron.py` | Cron: ставит requireResult=True на задачи Алины |
| На сервере: `/root/Bitrix24/ENVPro/proxy/task_require_result_cron.py` | Задеплоено на 155.212.143.68 |
| Лог: `/var/log/task_require_result.log` | На продакшн-сервере |

---

## Маркетинговая аналитика — архитектура (июль 2026, в работе)

**Статус:** ожидаем данных от маркетолога

**Файлы:**
| Файл | Содержание |
|---|---|
| `analytics/Пример отчета.xlsx` | Образец маркетингового отчёта (еженедельные задачи + бюджет) |
| `analytics/запрос данных.xlsx` | Запрос данных от маркетолога (отправлен 01.07.2026) |

**Согласованная архитектура Б24:**

1. **СП «Расходы» (1070) — расширить**, не создавать новый:
   - Добавить поле «Направление»: Выставки / Маркетинг / Операционные
   - Добавить поле «Канал» (для маркетинга: Посевы, Реклама, SEO...)
   - `parentId1048` — необязательный (у маркетинговых расходов нет)
   - Цель: единый фильтр/поиск по всем расходам компании

2. **СП «Статистика площадок» (новый)** — отдельно от расходов:
   - Площадка + Период (месяц)
   - Метрики: подписчики, охват, ER (← LiveDune API)
   - Метрики: клики, показы (← Яндекс.Директ / VK API)
   - Метрики: трафик, конверсии (← Яндекс.Метрика)
   - Заполняется cron-скриптом раз в месяц (снапшот)

3. **Компании** — завести подрядчиков: ИП Мельников, ООО ТЕЛЕКОТ, ООО ВК, ООО СМБ-Сервис

4. **СП «Блогеры»** — отложено, схема сотрудничества не определена

**Интеграции:**
| Канал | Способ | Статус |
|---|---|---|
| LiveDune | API (все тарифы, ~3300 ₽/мес = Business, 10к запросов) | Ждём API-ключ |
| VK Реклама | B24 BI Конструктор (нативно) + VK API | Ждём токен |
| Яндекс.Директ | API v5 (OAuth2) | Ждём токен |
| Яндекс.Метрика | B24 BI Конструктор (нативно) | Ждём ID счётчика |
| Telegram Ads | Нет API → ручной ввод | — |
| Посевы, Блоги, Сервисы | Ручной ввод маркетолога (до 5-го числа месяца) | — |

**Отложено до получения данных от маркетолога:**
- Создание СП и полей в Б24
- Cron-скрипты для записи статистики
- Дашборд /report/marketing
- Гипотезы/A-B тесты: шаблон задачи с requireResult (концепция согласована)

---

## Анализ зависших сделок и проектов (июнь 2026)

Для **Коммерческого директора** — поиск сделок и проектов РФ без активности 4+ недель.

- Крупнейшая зависшая сделка: **688М** (Рудишин А.Ю.) — последняя активность 14.05.2026, после КП ответа нет
- Анализ ведётся через b24proxy.bobp.ru → `crm.deal.list`, `crm.item.list` (СП)
- Отчёт: `arh-moskva-report.html` (обновляется)
- Схема CRM: `crm-schema.html` (обновлена)

---

## Что НЕ сделано / заблокировано

| Задача | Причина / Статус |
|---|---|
| Подключение сайта к Cloud Functions | Разработчик игнорирует |
| Тестирование CardScanner | Ждём сотрудников |
| Верификация Яндекс Cloud | Техническая проблема |
| Начальные остатки по точкам продаж (ID 10–18) | Не внесены |
| Стадии воронки Ателье (кат.0 Продажа) | Не настроены |
| Ателье-воронка: связь кат.0 ↔ кат.28 ↔ Отдел разработки | Не настроена |
| Расходы: исправить ufCrm24ExpenseType=1 на АРХ МОСКВА и GOTOVO | Неверные коды категорий (должны быть 210, 212 и т.д.) |
| Регламент: Выход ведущего создавать в стадии "Запланирован" при планировании | Документ не написан |
| Автоматизации (роботы) | Не настроены |
| Связь Сделка → Ведущий в карточке сделки | Оставлено намеренно: Ведущий виден со своей стороны (список сделок), со стороны сделки — нет |
| crm-schema.html — обновить с учётом СП Точки продаж | Схема устарела |

---

## Ключевые файлы

| Файл | Содержание |
|---|---|
| **`b24-api-patterns.md`** | **Главная база знаний по Битрикс24 REST API** |
| `system-overview.md` | Архитектура CRM (подробно) |
| `system-brief.md` | Краткое резюме системы |
| `arh-moskva-bi-brief.md` | Данные АРХ МОСКВА для BI-отчёта |
| `arh-moskva-report.html` | HTML-отчёт по выставке |
| `scripts/` | Все скрипты импорта |
| `mailings/` | Юридический анализ рассылок |
| `consent-service/` | Сервис согласий 152-ФЗ |

---

## Данные портала (02.06.2026)

- Контакты: 372 · Компании: 78 · Лиды: 47
- Сделки: 59 АРХ МОСКВА + ранее существовавшие
- Пользователей на портале: 22

---

## Важные грабли (не наступай снова)

1. `entityId` для полей = `CRM_{typeId}`, не `CRM_{entityTypeId}` → подробности в b24-api-patterns.md п.10
2. `crm.status.update/delete` — числовой `id`, не строка `STATUS_ID`
3. `editFormLabel` строкой не сохраняется — нужен `{"ru": "..."}` объект
4. `userfieldconfig.list` пагинирован по 50, `next` в корне ответа, enum не возвращается (нужен `userfieldconfig.get`)
5. `CATEGORY_ID` в `crm.deal.update` молча не меняет воронку — только через UI
6. `parentId1052` (camelCase) в `crm.deal.add` молча игнорируется — только `PARENT_ID_1052` через update
7. `userfieldconfig.add`: параметр называется `field` (не `fields`), внутри — **camelCase** (`entityId`, `fieldName`, `userTypeId`, `editFormLabel`)
8. `userfieldconfig.add`: нужен `moduleId: "crm"` — без него ошибка "{moduleId} not found"
9. `crm.type.update`: для UF-полей в новом СП сначала включи `isUseInUserfieldEnabled: "Y"` — иначе "Вы не можете создавать пользовательские поля"
10. При создании нового СП стадии авто-создаются (Начало/Подготовка/Согласование/Успех/Провал). Используй `crm.status.update` с числовым ID для переименования, `crm.status.add` с полем `STATUS_ID` для новых
11. `crm.status.add` требует обязательное поле `STATUS_ID` — без него ошибка "The field STATUS_ID is required"
12. `parentId1048` (lowercase) в `crm.deal.add` **молча игнорируется** как связь с выставкой — только `crm.deal.update` с `PARENT_ID_1048` (uppercase) работает. Фильтр `PARENT_ID_1048` в `crm.deal.list` при этом корректный.
13. Сделки ведущих (`PARENT_ID_1052: [ids]`) без дополнительного фильтра по датам возвращают ВСЕ их сделки за всё время — всегда добавляй `>=CLOSEDATE`/`<=CLOSEDATE` по датам выставки.
